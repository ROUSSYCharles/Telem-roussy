<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <h1>Fiche produit</h1>

    <ul>
        <li>nom du produit : {{ product.name }}</li>
        {% if product.description is not empty %}
        <li>description du produit : {{ product.description }}</li>
        {% endif %}
         <li>quantité en stock du produit : {{ product.quantityInStock }}</li>
         <li>prix du produit : {{ (product.price|price) }}</li>
         <li>
             <img src="/build/images/{{ product.imageName }}">
         </li>
         <li>ajouté au catalogue le : {{ (product.createdAt|dateFr) }}</li>
        <li>
            <a href="{{ path('product_show_all') }}">Lister tous les produits</a>
        </li>
    </ul>
        {% block body %}{% endblock %}
    </body>
</html>
